const classes = {
  default: 'badge',
  // class applied if color=green
  colorGreen: 'badge--green',
  // class applied if color=yellow
  colorYellow: 'badge--yellow',
  // class applied if color=orange
  colorOrange: 'badge--orange',
  // class applied if color=ref
  colorPurple: 'badge--purple',
  // class applied if color=gray
  colorBlue: 'badge--blue',
};

export default classes;
