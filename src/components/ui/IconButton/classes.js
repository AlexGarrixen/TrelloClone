const classes = {
  default: 'icon-button',
  // class applied if variant="contained" and color="default"
  containedDefault: 'button--contained-default',
  // class applied if variant="text" and color="default"
  textDefault: 'button--text-default',
  // class applied if variant="outlined" and color="default"
  outlinedDefault: 'button--outlined-default',
  // class applied if variant="contained" and color="primary"
  containedPrimary: 'button--contained-primary',
  // class applied if variant="text" and color="primary"
  textPrimary: 'button--text-primary',
  // class applied if variant="outlined" and color="primary"
  outlinedPrimary: 'button--outlined-primary',
  // class applied if variant="texcontained" and color="secondary"
  containedSecondary: 'button--contained-secondary',
  // class applied if variant="text" and color="secondary"
  textSecondary: 'button--text-secondary',
  // class applied if variant="outlined" and color="secondary"
  outlinedSecondary: 'button--outlined-secondary',
  // class apploed if disabled=true
  disabled: 'disabled',
};

export default classes;
